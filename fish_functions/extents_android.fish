set -gx PATH {$EXTENTS_PATH}/helpers/android {$PATH}
set -gx PATH {$EXTENTS_PATH}/helpers/mkbootimg_tools {$PATH}
set -gx PATH {$EXTENTS_PATH}/helpers/android/ferrari {$PATH}

set -gx PATH {$HOME}/works/android/aac2/dist/0.0.1 {$PATH}
