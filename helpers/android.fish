# mkbootimg_tools
set -x PATH $PATH {$WORKS_PATH}/android/mkbootimg_tools

# android generic helpers
set -x PATH $PATH {$EXTENTS_PATH}/helpers/android

# ferrari
set -x PATH $PATH {$EXTENTS_PATH}/helpers/android/ferrari
