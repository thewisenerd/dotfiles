#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '

export PATH=${HOME}/x-tools/arm-opi-linux-gnueabihf/bin:"${EXTENTS_PATH}/helpers/android/bin":${PATH}
